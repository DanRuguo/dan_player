# 26.0.5-snapshot.1 本机验证记录

日期：2026-09-06。当前交付范围是本机 Windows x64 **未签名预览便携包与安装器**，不签名、不提交或发布 GitHub。以下只记录本轮已有证据，不把 26.0.4 的历史检查计入本轮，也不将重叠测试组累加为独立测试总数。

本机日志目录为工作区根目录 `.migration/logs/2605-snapshot1/` 和 `.migration/logs/2605-followup/`。检查使用工作区独立数据目录、虚构封面和合成音频，不修改用户原音乐、歌单或设置。

## 最后截图修复

用户在完成下方 Computer Use 检查后，明确要求修复队列留白与全局消息并直接构建，无需再实测。此后的检查限于保存文件、代码、定向用例、构建和验包；不把此前候选的 Computer Use 结果描述为本批新改动的成品实测。

- 队列两侧不对称的原因是行自身左右 6 px 外，通用滚动条还固定占据右侧 24 px。队列改为在已有边距内绘制窄滚动条，保留鼠标拖动及原索引行为。`2605-snapshot1/queue-edge-tests.log` 的 12 项通过，包括 280／520 px 宽度左右几何相等、拖动不误播放、滚动后搜索仍选择正确原索引；两文件分析无问题，见 `queue-edge-analyze.log`。
- 多选加入队列、自动目录设置错误和桌面歌词三处失败消息仍使用旧全宽 SnackBar。现将全部生产调用统一至共享 `AppPresentationHost`；主程序旧文件保留导出入口，无 host 时也复用同一定宽主题气泡。保留关闭、撤销、时长、完整无障碍文本和可访问模式下的动作；已销毁 context 安全回退、新消息立即替换旧消息、动作重复点击只执行一次。`2605-followup/global-notice-main-tests.log` 的 33 项与 `global-notice-desktop-tests.log` 的 13 项通过；最后动作保护另复跑 13 项，属于重叠检查，不累加。最终共享实现分析无问题，外围只有 5 条既有命名 info。生产代码扫描只剩共享组件内部一个中央 SnackBar 兼容入口，见 `global-notice-call-sites.txt`。
- 只读检查独立 profile 的 JSON：三个智能歌单及规则、一个含两首歌曲的普通歌单、随机与列表循环偏好、两个逐曲位置、自动目录更新和全部本地音频续播设置已保存。收据为 `2605-followup/saved-results-review.json`；没有改写资料或关闭仍可由用户操作的 QA 实例。
- 便携装配补齐当前版本验证、研究、Computer Use、发行和 ReplayGain 文档，保留原文件名，使验证记录中的本地链接在解压后可以打开。装配脚本语法检查通过，最终载荷以验包收据为准。
- 用户追加未签名安装器后，第一次 Inno 前置检查因新增五篇文档不在原生文件白名单中中断，日志保留于 `unsigned-final-installer-build.log`。仅补入这五个明确文档名，未放开任意根文件或停用校验；新增接受及未知文档／路径拒绝回归，`unsigned-installer-native-policy-tests.log` 的 39 项通过。生产安装器 DLL、cleanup 增量构建并以新未签名副本打包，未借用旧白名单产物。随后只重新装配说明文档，不重复编译未变的播放器和桌面歌词；最终便携与安装器均基于同一载荷核对。
- 用户追加要求加快返回并统一动画节奏。主程序与桌面歌词共用 120／180／240／300 ms 四档，页面进出均为 240 ms（返回原为 420 ms），通知及控件反馈 120 ms、普通切换及弹窗 180 ms。歌词视觉过渡分为 480／600／720 ms，采样位置平滑 60 ms；字词高亮仍由音乐时间决定，启动总时长及“下一首播放”520 ms 曲线保留。`2605-followup/motion-group-tests.log` 的 176 项通过，覆盖返回状态／连续反向、减少动画、入场、歌词滚动、进度控件、提示、语言、主题及插队曲线；组内复用了早前通知等检查，不与其他记录累加。主程序分析只有 3 条既有类命名 info，桌面及共享实现最终分析无问题。
- 活动入口补查发现卸载确认仍绕过统一弹窗，以及部分前置翻译的错误文本在非中文界面被当成普通提示。现统一卸载容器并保留原边距，错误／成功／警告显式分类；CUE、M3U、更新、主题和分类封面等入口保留翻译参数。`active-entry-tests.log` 首轮 18 项通过，3 项新增语义用例最初因测试句柄释放过晚失败；改正夹具后 `active-entry-final-tests.log` 仅复跑该 3 项全部通过，共 21 项不同用例。没有调用真实卸载器或文件选择器。
- 剩余前置翻译及 M3U 导出错误调用补齐明确类型后，`notice-callsite-tests.log` 的更新、保存失败、分类、Shell、歌单互通等 89 项通过，包含前述入口的部分重复检查；相关文件分析无问题。
- 内置 QQ／网易搜索此前只限制响应块间隔，持续慢分块可能长期占用连接。新增每次请求从连接到正文结束的 12 秒截止时间，取消会强制关闭所属客户端并阻止重试；原先最多 2 次尝试及 220 ms 退避保留，**不是所有来源合计 12 秒预算**。封面在创建 `ImmutableBuffer` 后的取消／配置检查移入已有 `try/finally`，保证异常路径也释放缓冲。`online-search-lifecycle-tests.log` 的 59 项通过，其中新增 11 项覆盖慢分块、响应头／正文取消、预取消、不同请求隔离及重试等待取消；其余为既有服务／封面用例。5 文件分析无问题。使用假客户端和合成数据，没有重新请求真实音乐服务，也未进行内存堆实测。

本轮不是全仓库安全或底层穷尽审计。补查仍保留的维护项：未调用的 Win32 解锁原型含旧别名；旧设置迁移和 Windows 背景回退有实际兼容用途；异常云盘／断连共享的原生本地 I/O 仍可能等待操作系统返回，两个工作线程同时滞留或退出等待需专项验证。本机没有复现该类挂起，未在本次收尾中更换音频引擎或删除兼容层。只读记录位于 `legacy-alias-review.md`、`active-entry-review.md`、`active-native-review.md`，搜索修复状态以 `online-search-lifecycle-fix.md` 为准。

## 追加修改与验收范围

- 音乐、艺术家、专辑、分类、文件夹、歌单、智能歌单预览及歌词大小版共用随机／循环状态。按用户最新更正，歌词页使用独立透明图标，其余歌曲页采用连体双段外观。按钮不开始播放、不用当前页面内容替换队列；开启随机会调整现有队列顺序并映射当前条目。循环顺序为关闭→列表→单曲→关闭，与随机开关独立。多选播放及随机播放所选保留。
- `2605-snapshot1/playback-mode-ui-result.md` 汇总 131 项不同定向用例通过及 25 文件分析无问题；覆盖页面实际入口、全局同步、空队列预设、被动初始化、三倍英文文字、窄屏、点击歌曲和多选播放的排序／重复条目规则。重复执行不再累计。
- `2605-snapshot1/playback-modes-report.md` 记录底层 51 项定向检查通过及分析无问题；自动推进遵从循环关闭的停止边界，列表循环才进入新随机周期。模式切换不调用播放或跳转，并处理加载中的目标索引、重复条目、空队列持久化。
- 新增默认关闭的按曲进度记忆，支持长音频／全部本地音频，独立保存于 `track_resume.json`，可在设置清除。自动下一曲、循环、会话恢复和显式定位各自保留原来的位置规则；读取超过 500 ms 则跳过，迟到结果不再跳转。`2605-snapshot1/track-resume-tests.log` 的存储／设置／备份 12 项，以及 `track-resume-restore-final.log` 的恢复策略 4 项通过；10 文件分析无问题。`track-resume-lifecycle-tests.log` 中另有 22 项既有模式／会话／书签回归通过，其余 3 项恢复用例与最终日志重叠。这些日志属于文件、策略和 Flutter 测试；后续成品 Computer Use 已补测全部本地音频模式的逐曲续播、上一曲／下一曲从头播放及重启恢复，范围见下文。
- 本地搜索采用分批评分、有限候选排序及查询取消；艺术家和专辑索引也分批构造。`2605-followup/search-final-tests.log` 中 11 项索引／数据检查通过；当时页面用例受并行修改中的临时编译问题阻挡。修复后 `2605-snapshot1/search-review-after.log` 的 7 项异步搜索及 4 项页面交互全部通过，包括元数据尚未发布时的排序键快照、全部联网源关闭后的早期错误观察与页面重试。两组索引用例存在重叠，不能相加为独立总数；修复前真实失败分别保留于 `search-review-before.log`、`search-provider-before.log`。
- `2605-followup/resume-preferences-disk.log`：新增续播设置真实磁盘保存／重新读取／旧配置默认关闭 1 项通过。四语完整性用例首次被临时 import 位置问题阻挡，修复后 `catalog-final-fixed.log` 的该项通过。`integration-analyze.log` 及 `2605-snapshot1/search-review-analyze.log` 无 error/warning，仅保留原有的设置兼容方法和搜索 key 命名 info。
- 后续轮次已获得 `node_repl` 并成功导入 `@oai/sky`，成品 Computer Use 检查已进行，仅使用独立的 `tool/qa-2605-cu/profile`。此前坐标脚本继续停用；Flutter 定向测试、成品界面交互与尚未完成的真实 Explorer 悬停验收分别记录。

## 当前 Computer Use 成品检查

检查使用 `060043`、`062900` 两批未签名包及独立的 `tool/qa-2605-cu/profile`，全部音频与封面为测试素材。操作过程和证据索引见 [Computer Use 成品检查记录](26.0.5-snapshot.1-computer-use.md)。以下通过项限定于实际完成的操作，不表示所有布局、音频输出或网络接口均已实测。

| 范围 | 当前结果 |
| --- | --- |
| 全局播放模式 | 音乐、歌词、专辑及自定义歌单页面状态同步；模式按钮仅切换状态，不开始播放。已检查随机与列表循环共存，以及循环关闭→列表→单曲的三态切换。 |
| 歌词模式图标 | 已实际检查恢复后的两个独立透明图标；暗／亮主题可见，Amber→Cyan 动态换色跟随主题。 |
| 队列查找与整理 | 搜索 `3` 后显示 `1/4`，定位当前曲会清空查询。去重 `4→3`、撤销 `3→4` 成功；仅保留当前曲 `2→1`、撤销 `1→2` 成功。保存当前队列为普通歌单后含 2 首歌曲，封面使用首曲。 |
| 智能歌单 | 三种听歌记录预设已预览并保存；其中近期播放结果为 5 首，从未播放结果为 1 首。 |
| 曲库与封面 | 通过文件夹管理添加 `feature-media` 测试目录，合成 FLAC、MP3 的两张封面可见；艺术家和专辑入口显示首曲 Cyan 封面。 |
| 自动目录更新 | 通过设置开启目录监测后，向独立测试目录加入一首合成 WAV，曲库自动由 `5→6`，无需手动刷新。 |
| 逐曲续播与会话 | `allLocal` 模式下，一首歌曲播放至约 15 秒后切换，再点击该曲从约三分之一处继续；上一曲／下一曲从 0 开始。退出并重启后暂停会话和设置保留。 |
| ReplayGain 设置 | 关闭／曲目／专辑模式以及峰值保护控件状态检查通过。这是成品界面检查，未进行 CU 音频 DSP 测量；PCM 证据仍以原生测试段为准。 |
| 搜索 | 本地搜索 `3` 找到 2 首歌曲，“总乐库”分类切换可用。联网来源在测试 profile 中禁用，此次不算 API 可用性重测。 |
| 文件夹路径显示 | `062900` 包已实际确认长路径末尾目录名可区分，修复了两条路径只显示相同前缀的问题。 |

通知避让已在最终 `064851` 包通过 CU 复测：旧实现上移约 32 px，第一次修复后仍多上移约 20 px，原因是通知预留区重复计算了弹窗底部外边距。第二次修复后现场仅上移约 4–5 个物理像素，保留必要避让，连续点击不会大幅跑位；原弹窗底部与通知顶部约隔 7 个物理像素，小于 125% 缩放下的 8 个逻辑像素（10 个物理像素）安全间隙，因此不将现场结果描述为完全零位移。无通知布局保留；有足够空白时不移动的边界由完整队列布局、子导航器及 125% 缩放定向用例覆盖。该项连同一般弹窗的 11 项测试共 12 项通过，9 文件分析无问题。日志为 `2605-snapshot1/queue-notice-inset-test.log`、`dialog-notice-inset-tests.log` 和 `dialog-notice-inset-analyze.log`。

以下仍未完成：真实 Explorer 悬停缩略图／Peek（当前 CU 没有可用的悬停 API）、实际改变窗口尺寸的拖动检查，以及窄版歌词页 CU 检查。动画已有定向曲线测试，本轮未做逐帧 Computer Use 观感评估，不据此声称该项实机通过。

以下保留本版本首次便携批次的检查记录，作为未变动功能的已有证据；追加修改重新构建与交付核对以根目录 `DEVELOPMENT.md`、`.migration/latest-package.json`、`.migration/logs/unsigned-player-verification.json` 为准。

## 已完成的定向检查

| 范围 | 结果与证据 |
| --- | --- |
| 主题色操作图标、播放控件、工具栏及窄屏布局 | `controls-tests.log`：166 项通过；包含明暗主题、真实窄视口和 200% 文字。最终调整的“下一首播放”曲线另见 `flight-revised-tests.log`：3 项通过，覆盖精确端点、平缓抵达、后续指针输入立即取消。两个日志的动画用例有重叠。 |
| 分类封面及图片管线 | `artwork-flutter-tests.log`：41 项通过，覆盖七类分类首曲封面、自定义封面移除回退、同秒文件指纹及旧图标缓存隔离。`artwork-native-after.log`：10 项 Rust 测试通过，包含 ID3v2.3/v2.4 合成标签、像素/内存边界、只读字节不变及 Windows 文件图标过滤。修复前的失败保留于 `artwork-native-before.log`。 |
| 任务栏缩略图、Peek 配色与排版 | 最终 `taskbar-centered-tests.log`：33 项通过；涵盖明暗配色、较大 Peek 独立构图、同秒封面变化、字节上限以及最新代缩略图/Peek 成对发送。早期 `taskbar-tests.log` 的重叠用例不重复计数。 |
| 队列搜索与可撤销去重 | `queue-search-tests.log`：22 项通过；覆盖 645 首队列的过滤操作索引、保留当前重复条目、定位被过滤的当前曲、UNC 路径别名、在线身份、CUE 分轨与原整轨区分、撤销及 280 px/3 倍英文文字布局。此组与通用控件组中的既有队列检查有重叠。 |
| 听歌记录智能歌单 | `smart-history-new-tests.log`：新增 7 项通过；覆盖旧 JSON、空/清空记录、时间窗口、排序后结果上限、重复录音身份与 CUE、求值快照、仅必要历史通知刷新、窄屏中英文保存。原有 13 项智能歌单测试在较早合并运行中通过；该次新界面夹具的失败已由上述最终日志关闭。 |
| 自动监测音乐目录 | `library-watch-controller-final.log`：最终 11 项控制器/引用测试通过；包含合并与最大等待、扫描期间事件、忙锁竞争、离线恢复、取消失败、探测超时/退出、失败仅重试一次及歌单引用保留。`library-watch-tests.log` 中设置 UI 的 1 项窄屏英文/保存失败测试通过，控制器重叠项不再累计。 |
| ReplayGain 与音频打开生命周期 | `replaygain-unit.log`：27 项通过，涵盖标签读取、增益/峰值约束、无标签复位及非阻塞打开/独占设置启动；`gain-ui-tests.log`：3 项设置交互测试通过，包含无需初始化音频即可打开、模式/峰值保护独立设置和窄屏翻译。 |
| 自动刷新下的索引与备份互斥 | `library-concurrency-fixes-tests.log` 中时长校准 10 项及真实索引实例替换落盘 1 项通过；其中备份夹具初次因 import 位置编译失败，修复后 `cache-backup-gate-tests.log` 的 2 项通过。验证备份 flush/export 全程持有同一互斥门及失败可重试。 |
| 已打开标签编辑器跨曲库刷新 | `metadata-refresh-tests.log`：14 项通过；包括编辑草稿跨实例替换、已提交重命名同步当前索引、保留新扫描字段/未保存歌单名称与条目身份，以及保存失败/同步重试。 |
| 多选跨曲库刷新 | `selection-refresh-tests.log`：8 项通过；选中项按稳定身份绑定新实例，仅实际消失项移出，批量操作继续使用当前元数据与可见顺序。 |
| 设置持久化与四语界面 | `settings-localization-tests.log` 首轮 32 项通过、1 项缺少“刷新”翻译；补齐三语后 `localization-final.log` 单独复跑该翻译完整性检查通过。未重复运行已经通过的磁盘持久化用例。 |

## 原生验证的实际边界

- 任务栏原生 Release 测试目标构建成功，见 `taskbar-native-build.log`。`taskbar-result.md` 记录 8,059 项缩略图策略断言、15 帧合成图的 184 项 DIB/像素/生命周期断言，以及 36 个隐藏 HWND 的 1,960 项几何断言通过。`taskbar-native-qa.log` 是合成渲染辅助程序的构建记录。已查看虚构封面的明暗缩略图与 Peek 输出；这些结果证明生成、布局与资源边界，不等同于真实 Explorer 悬停/Peek 验收。
- `replaygain-native.log` 的 1 项原生集成测试通过。合成 FLAC、Ogg、ID3v2.3 MP3、ID3v2.4 MP3 分别经过普通解码、tempo、独占路径所用混音器，共 12 组 PCM 检查；记录的归一化 RMS 比例均为 0.5。另覆盖生产播放器设置切换、静音、过期打开拒绝、无标签/关闭模式复位及重开流。
- 本机上述 ReplayGain 探针明确返回 `hardwareExclusiveAvailable: false`，设备当时被占用。已验证相关混音管线及失败处理，**不声称独占声卡实际输出成功**，也未覆盖所有声卡、采样格式或驱动。
- 目录监测组中 1 项确实使用本机 Windows `Directory.watch`，在独立目录创建合成文件并收到事件；其余失联、超时与事件调度用例采用注入事件和假时钟。没有据此声称所有 USB 拔插、网络共享或文件系统行为都已实机验证。

## 分析、构建与装配

- `integrated-analyze.log` 对改动代码及相关测试、共享翻译做整合分析，无 error/warning。发现的两处新增花括号 info 修复后，`analyze-final-fixes.log` 对受影响文件报告 `No issues found`；其余 9 条为已有命名 info。各功能定向分析另见 `player-features-analyze.log`、`artwork-analyze.log`、`queue-search-analyze.log`、`replaygain-pipeline-analyze.log`、`taskbar-test-analyze.log`。
- `unsigned-build.log` 记录本次主程序 Windows Release 构建成功，136.0 秒；共享翻译更新后的桌面歌词 Windows Release 构建成功，41.9 秒。两者均来自本次构建，不借用旧版本主程序。
- 同一日志记录两项目 AOT 所需图标、字体、品牌资源核对无缺失，并核对九个 UN4SEEN x64 运行库及原始声明；完整便携目录已装配。首次最终验包因缺少本版本 `VALIDATION.md` 中断，本文件用于补齐该文档，**不把这次中断描述为验包全部通过**。补文档后可继续核对同一批构建产物，无需为文档重新编译程序。

## 历史成品隔离 QA 与交付核对

本节 `tool/qa-2605/` 是首次便携批次的历史记录；后续已改用 `node_repl`／`@oai/sky` 和独立的 `tool/qa-2605-cu/profile` 完成上述 CU 补测，不以旧批次结果替代后续包检查。

- 完整便携目录已使用 `tool/qa-2605/profile` 启动，恢复四条合成音频队列（包含重复条目），当前索引 2、暂停位置 3 秒。主界面、队列、歌词详情及自动刷新设置页面可见。正常关闭测试进程后，队列、索引、暂停位置以及自动刷新和 ReplayGain 设置均保留；见 `tool/qa-2605/packaged-smoke.json`。
- 成品自动监测实际合成 WAV 的新增、重命名和删除，三次索引更新均完成，见 `tool/qa-2605/watch-packaged-smoke.json`。该探针仅检查自己的测试目录。初次队列磁盘比较发生在索引更新后，不把它扩展为所有异步时序的证明；相关并发和引用行为由上表定向测试覆盖。
- 该历史批次的坐标脚本未可靠完成去重／撤销点击，不将该次交互算作通过，也不根据未确认的点击行为判定播放器故障。用户随后指定界面测试使用 Computer Use，坐标点击已停止，自有测试进程正常关闭。后续 CU 已完成队列查找、去重／撤销与保留当前曲／撤销；真实 Explorer 悬停／Peek 仍未完成，详见当前检查段。
- 文档补齐后直接使用同一批主程序和歌词构建重新装配，不为文档重编译。交付核对采用 `verify_local_release.ps1 -UnsignedCandidate`：检查 ZIP 与逐文件摘要、源码/构建对应关系、自有三个二进制为 `NotSigned` 以及原厂运行库。最终结果写入工作区 `.migration/logs/unsigned-player-verification.json`；只有 `Passed: true` 的收据对应当前交付包。
- 最终目录、ZIP 和安装器的大小／SHA-256 记录于根目录 `DEVELOPMENT.md`、`.migration/latest-package.json` 和 `.migration/latest-installer.json`，不写入包内本文件，避免摘要自引用。未签名安装器采用同一便携载荷，构建及校验后保存，不运行安装器或安装／卸载实测。GitHub 正式版仍保留 26.0.4。

功能规则、参考项目实际源码/许可证与使用边界见 [播放器源码研究与实现说明](26.0.5-snapshot.1-player-research.md)。本轮不是新的在线 API 全量探测，也不沿用旧版网络探针作为当前服务可用性的证明。
