# 26.0.5-snapshot.2 验证

本轮基于本机快照 1 工作树开发，所有测试使用工作区独立资料和虚构素材。既有正式版或快照 1 的实机记录不计为本版实测。

| 模块 | 本轮执行与结果 |
| --- | --- |
| 身份与旧统计 | 56 个相关用例通过：重复路径读取、改标签、不同文件、CUE 边界、唯一／歧义归属与总额保护 |
| 搬库及备份 | 32 个集中用例通过：预览、CUE 映射、故障注入、幂等重放、快照校验、成组恢复与缺失歌曲保留 |
| 播放 | 会话代次、旧位置／完成事件、停止分类、队列与输出策略用例通过；末轮加入实际 BASS DLL 的无物理输出合成 PCM／CUE 探针 |
| 歌词 | 存储、锁定、来源竞态、单次偏移、历史恢复、写入失败及工作台控件回归通过；含窄布局与大字号 |
| 顺序及备份边界 | 自定义排序 5 项、别名与不同文件身份 2 项、临时同步记录保护 1 项通过；智能歌单歧义历史 6 项通过 |
| 集成收尾 | 根模块集中 77 项通过后修正歌单首页旧图标数量 fixture；最后模式入口与同步互斥 2 项、播放重命名 1 项、损坏主文件及备份同步 1 项通过。相关静态分析无错误，BASS 保留 4 项原有命名提示 |
| 统一 EXE | 入口分派、歌词进程通信／重启／EOF、主题和调色板及版本一致性 88 项通过；8 个 Dart 文件分析无问题。原生入口策略 11 项无 UI 检查通过；安装布局兼容与回退 40 项通过，未生成安装器 |
| 构建与布局 | 此前未签名统一主程序 Windows Release 构建 84.2 秒成功，包含歌词与调色板入口，AOT/字体/图标核对通过；EXE ProductVersion 与 FileVersion 均为 26.0.5-snapshot.2。新旧包布局及版本错配 12 项静态验证通过；发布前 UI 改动另行重建 |

日志集中位于工作区 `.migration/logs/2605-snapshot2/`；歌词日志为同级 `2605-snapshot2-lyrics-*` 等文件。不同组有重叠，不累计为全项目总数。

## 发布前 UI 收尾

- 歌词校准与锁定：放大并居中标题，拆分时间校准、匹配保护和内容版本区；操作间保留至少 12 px 间距，主要按钮至少 44 px 高。小窗口可滚动，大字号标题在独立整行展示。
- 文件夹管理：居中标题、说明、空状态和底部操作，短列表收紧、多列表滚动；完整路径左对齐并可选择或复制。保留原有草稿、取消、确认、扫描互斥和失败恢复逻辑。
- 工作台 8 项针对性用例通过，包含保存、失败、输入和实际生产主题布局；最后用 Windows 平台主题复测中英文 5 个布局场景，全部通过。目录控件 8 项及原刷新流程 27 项通过。相关代码分析无问题。
- 使用 Entry 生产主题、Windows 平台样式、应用字体和 Material Symbols，以虚构曲目和目录渲染并逐图检查。覆盖明暗、长路径、无目录、多目录、短窗口、320 px 宽／200% 字号的中文目录，以及英文 200% 字号的歌词（400 px 宽）和目录（480 px 宽）界面，未发现按钮碰撞或横向溢出。既有语言完整性和占位符检查 27 项通过。

渲染图保存在工作区 `tool/qa-2605-snapshot2/ui-final/`，日志为 `lyric-workbench-ui-final-*` 和 `library-folders-ui-final-*`。这是生产控件离屏验证，并非 Computer Use 或成品实机测试。

发行检查发现旧书签测试桩未同步稳定 ID 的可选参数，已修复并补充不同歌曲书签不会串入的断言。随后一次完整 Flutter 回归中的失败按原因集中处理：迁移 fixture 使用混合分隔符、旧版统计备份轮换预期、100 ms 的旧动画等待、图标主色旧预期，以及移动端选择手柄导致的桌面按钮测试误判。保留原有功能断言，未为这些问题修改生产播放或数据逻辑；迁移／恢复 25 项、播放／书签／多语言布局 110 项、短窗口弹窗 11 项分别通过。新版界面另补齐英语、日语和韩语模板，实际发布内容以 GitHub 对应提交的 Windows CI 和发行附件为准。

## 合成曲库基线

Windows 11、i7-14700KF（28 逻辑处理器）、约 64 GiB 内存、Dart 3.13.1／Flutter test JIT。同一进程顺序执行 1千／1万／10万条虚构元数据，含 1% CUE；不扫描磁盘、不解码音频或封面。每组 21 个搜索样本，性能数字不等于真实播放器帧率。

初测定位到搜索 LRU 删除/重插后淘汰会反复扫描旧位置，改为固定 8192 容量的链表缓存。必要复测中，10万条索引重建由 33,245.579 ms 降至 8,987.983 ms；1万条为 890.661 ms。10万条搜索 p95 为 87.937 ms（初测 382.475 ms），进程峰值 RSS 约 372 MiB。只有一次前后样本，查询本身未改，不能把全部时差归因于缓存修改。

缓存、异步搜索与规模用例合计 11 项通过；一个新用例按文件名排序的 fixture 已修正并单独复测。日志 `scale-baseline.log`、`search-lru-scale-recheck.log`、`search-lru-regression-final.log`。复现：设置隔离 `DAN_PLAYER_DATA_DIR` 后执行 `flutter test --no-pub --dart-define=DAN_RUN_LIBRARY_BASELINE=true test/library_scale_baseline_test.dart --reporter expanded`；默认跳过，不加入每次例行全量检查。

**未执行**：Computer Use 界面操作（本任务工具列表没有 `node_repl`）、统一 EXE 成品下的歌词置顶／穿透／调色板人工操作、真实设备拔插／休眠恢复、物理输出连续性、长时间内存和音频负载观察。模拟后端与无输出原生探针不能替代这些检查。

条件项的未纳入范围、迁移与回退说明见 [更新说明](release-26.0.5-snapshot.2.md)。
