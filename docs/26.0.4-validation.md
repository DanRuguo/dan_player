# 26.0.4 本机验收说明

2026-09-05，26.0.4 正式版检查记录。此前的未签名包是用户验收阶段产物；本次在完成发布前任务后统一构建、签名和装配正式版。

检查聚焦改动：开屏/搜索交互、详情播放控制、QQ搜索与评论解析、自定义源能力、队列编辑与保存、A-B循环、Windows任务与卸载交接，以及多选、M3U8、书签、智能歌单、CUE分轨、队列撤销和网易增强 API。共享翻译改变后，主程序和桌面歌词都重新构建。装配、签名及合成音频实机结果保存在本机工作区根目录 `DEVELOPMENT.md` 与 `.migration/logs/`，区分本机实测、历史检查和持续集成结果。

## 发布前追加验证

- 智能歌单 6 项条件/存储测试、7 项界面测试通过；追加存储与路径迁移组共 10 项通过。覆盖实时筛选、取消过期预览、失败保留规则、原子保存、英文窄屏大字号与表单保存期间的操作保护。
- CUE、会话、删除/元数据及后台打开共 30 项相关回归通过；实际生产分轨函数的原生用例另 1 项通过。五种原生解码/倍速/混音组合验证片段边界与相对跳转，详见 [CUE 说明](cue-tracks.md)。
- 队列撤销相关组 23 项通过，覆盖重复歌曲的确切位置、随机顺序备份、批量插队、撤销上限及换曲清空。
- ARTEISIA API/歌词相关 52 项通过，1 项旧的可选在线探针未启用；歌词选择与工具栏组 65 项通过，新 CUE/智能歌单入口另 1 项通过。实际 96 秒短版未查得，34 个在线候选均被版本门槛排除，没有发送错误版本歌词请求，详见 [逐来源实测](26.0.4-artesia-api-check.md)。
- 首次 Windows CI 暴露的 8 个失败已修复：4 项旧存储测试的路径调用门控、2 项过期搜索界面断言、2 项英文能力标签溢出。未降低原断言范围；对应存储/迁移 44 项、设置 38 项、搜索界面 12 项回归通过。最终提交的 CI 结果以 [Windows CI](https://github.com/DanRuguo/dan_player/actions/workflows/windows_ci.yml) 和本机开发文档记录为准。
- 后续 CI 的 2730 项通过、2 项失败中，修复了编辑过协议的歌词源被错误要求获取元数据，以及 CUE 确认按钮遗漏翻译；对应 14 项协议回归、30 项多语言/CUE 界面测试通过。用户指出的 A-B 工具栏偏移改为垂直居中，6 项队列交互/布局测试通过，覆盖 1–3 倍字号与窄窗口换行。
- 多选/选择生命周期 11 项测试通过，覆盖按显示顺序批量操作、刷新后选择保留、隐藏选择提示及窄窗口菜单。
- 网易增强 API 及相关设置共 37 项测试通过；真实部署固定上游提交，按固定录音验证搜索、详情、封面解码、歌词、两类评论两页和音频前缀。详情见 [独立 API 报告](26.0.4-api-release-additions.md)。
- 播放书签 6 项数据/交互测试通过，另检查明暗主题真实字体渲染、360×520 窗口和 1.8 倍字号。
- 歌单与迷你歌词的一组相关测试 121 项通过；随后 M3U8 边界修复的 5 项路径测试和最终 8 项文件/交互测试通过。包括重复项、跨盘/UNC、编码后控制字符、HLS 整体拒绝、导入确认、错误选择器、不支持的导出项目与目标文件保护。
- 主程序 `lib test` 和桌面歌词 `lib` 整合分析通过：无 error/warning，分别保留 18、16 条既有命名/弃用 info。不同测试组存在重叠，不累加冒充独立总数。
- 两个 Windows x64 Release 最终构建通过；AOT 内所需 Material 图标、字体与品牌资源核对无缺失。最终签名便携包/安装器使用同一批构建输出，并核对文件清单、哈希、版本与证书指纹。
- 新原生 Windows shell 策略测试纳入 Windows CI，与本轮既有本机 33 项策略断言及隔离冷/热启动实测相对应。

## 本轮重点

- 两个品牌展示各减少 0.5 秒，总时长约 2 秒，保留淡入淡出与辅助功能。
- QQ 评论支持不透明评论 ID；QQ 搜索使用本轮验证有效的公开搜索接口。
- 内置来源按已知评论能力显示分类；自定义来源先使用默认评论列表，再根据声明开放排序。
- 搜索按钮、清空、等待提示和过期请求取消；分类栏采用带图标的主题色圆角分段样式。
- 歌词详情播放控制透明描边；进度条使用圆角竖条手柄、交互形变和时间预览。隐藏页面停止监听进度；播放时间更新合并订阅，减弱动画时直接响应。
- 队列按条目整理、保存为歌单；本地歌曲 A-B 片段循环。
- Windows 任务与单实例指令转发；设置内打开卸载向导，先完成退出交接，保留音乐和用户数据。
- 修复冷启动“下一首”被尚未完成的源打开过程吞掉：恢复调用共享真实完成 Future，失败也释放任务队列，不增加轮询。
- 多选菜单补齐真实批量动作并在曲库通知后保留状态；M3U8 解析/编码在 isolate 执行，有体积与数量上限，不递归访问文件或网络。
- 命名播放书签按需加载并串行原子保存；切歌后禁止把旧曲目的位置应用到新曲目。修复详情页音量通知器未释放。
- 迷你歌词使用先淡出后淡入，避免新旧两段文字重叠；补充纯音乐标记识别。

## 检查边界

- 公开音乐接口的搜索、歌词、评论和播放/下载权限不同；具体曲目受版权、地区、账号与服务状态影响。成功搜索或取得封面不表示完整歌曲可以播放或下载。
- 对“卡农、稻香、Good Time”的逐来源、逐能力结果见 `26.0.4-api-compatibility.md` 和 `26.0.4-api-release-additions.md`。自托管服务未配置时不宣称已联网可用；试听与未命中原唱明确记录，不用翻唱替代原唱成功结果。
- 正式发行使用新 `CN=RCEIT.Inc` 十年自签名证书，指纹 `492AD4FCB49C9898B6B98251D4FE9FE78BDB56C1`。签名者及内容完整性与系统公信力分开验证；未申请公共 CA 证书，也未改变系统信任库。旧证书版本需手动下载安装器升级。
- 自动化/实机检查使用独立测试目录与合成音频，不改用户音乐，不执行真实卸载。QA实例不注册系统 Jump List；正式正常启动会注册。
- 不承诺覆盖所有显示器、声卡、触控设备或互联网上所有 API 的所有曲目。

## 功能使用与升级

### 歌单与播放

- 统一多选菜单：播放所选、随机播放、下一批播放、追加队列、加入歌单、导出 M3U8、复制歌曲信息/本地路径、全选与反选。按显示顺序执行，保留重复曲目；曲库刷新不再清空选择。
- 新增 M3U/M3U8 导入及 UTF-8 M3U8 导出，可选相对路径，保留顺序、重复项和 EXTINF 信息，支持跨盘与 Windows 网络共享路径。导入先预览，不复制音乐，不递归读取嵌套歌单。
- 播放详情“更多 → 播放书签”保存命名的时间位置或 A-B 片段，支持恢复、重命名、删除及跨次启动保留。本地歌曲的 A-B 循环可从队列顶部设置。
- 队列可以移到下一首、移除条目、定位当前曲、仅保留当前曲，以及保存为新歌单。
- 新增智能歌单：保存关键词、艺术家、专辑、格式及最短/最长时长条件，曲库刷新后按规则更新结果，支持排序与常用播放操作。
- 新增 CUE 分轨导入：整张音频以独立曲目播放，分轨有自己的进度与边界，保存到普通歌单并可恢复队列；不拆分原始文件。
- 队列整理最多撤销 10 步，保留当前歌曲、暂停状态与进度；切歌或替换整个播放队列后清空历史。

### 联网与评论

- 更新 QQ 搜索兼容方法，修复本轮遇到的 2001 服务状态和不透明评论 ID 解析错误。不同平台按实际能力显示评论列表、热门或最新排序。
- 新增网易云增强 API 的专用适配器及默认关闭的预设，支持搜索、详情、封面、歌词/翻译、热门/最新评论、播放与独立下载解析。使用前需部署或填写实际服务地址。
- 对“卡农、稻香、Good Time”按来源与固定录音核实能力。增强 API 的《Good Time》原唱当前只有 30 秒试听，播放器不会把它当完整歌曲；《稻香》未命中已确认的周杰伦原唱，未用翻唱冒充成功。
- 搜索不逐首追加详情请求；请求可取消并有时间及体积上限。搜索按钮、清空和忙碌反馈补全，旧响应不会覆盖新结果。
- 默认歌词区分未提供歌词、连接/解析失败和保存失败；自动匹配检查版本与时长，手选保留版本提示，酷狗不再以明显属于其他歌曲的歌词候选兜底。
- 使用《結想は花となる short ver.》96 秒录音排查搜索、歌词、评论与封面：本次未找到可确认的同版结果，195 秒完整版的可用结果单独记录，不自动套用到短版。

### 界面与 Windows

- RCE 和 DanRuguo 启动画面各缩短 0.5 秒，品牌展示总计约两秒。
- 搜索分类改为带图标的圆角分段导航；歌词详情播放按钮改为透明描边。
- 进度手柄改为圆角短竖条，加入悬停/拖动形变、时间预览和松手回弹；尊重减弱动画设置，隐藏页面停止进度监听。
- 修复迷你歌词切换时新旧文字重叠，以及详情音量监听资源未释放。
- 修复窄窗口下第三方 API 能力标签的长英文溢出，保持能力名称完整可读。
- 任务栏/开始菜单右键提供主窗口、播放暂停、上一首、下一首和迷你播放器。重复打开复用已有实例；修复冷启动快捷任务被会话恢复吞掉。
- 设置 → 更新与关于 → 应用管理可打开本安装的卸载向导，便携版提供程序目录入口。

### 下载与升级

提供 Windows x64 安装器、完整便携 ZIP 和 SHA256SUMS。便携包需完整解压后运行 `Dan Player.exe`。

本版自有程序使用新的 `RCEIT.Inc` 十年自签名证书（2026-09-05 至 2036-09-05）。它不等于公共 CA 信任或 SmartScreen 认证，Windows 仍可能提示。旧版固定了旧证书指纹，从旧证书版本升级请手动下载本版安装器；新安装器支持原位升级。安装器不会自动安装信任证书。

公开 API 的可用性随曲目、地区及服务状态变化，搜索成功不表示具有完整播放或下载权限。标准歌单交换和书签限定本地音乐。

相关资料：[内置 API 实测](https://github.com/DanRuguo/dan_player/blob/v26.0.4/docs/26.0.4-api-compatibility.md)、[新增 API 实测与配置](https://github.com/DanRuguo/dan_player/blob/v26.0.4/docs/26.0.4-api-release-additions.md)、[Echo/ZeroBit 与其他播放器研究](https://github.com/DanRuguo/dan_player/blob/v26.0.4/docs/26.0.4-final-player-research.md)、[M3U8 说明](https://github.com/DanRuguo/dan_player/blob/v26.0.4/docs/playlist-m3u8.md)。
